# =============================================================================
# requirements.txt - 全局依赖说明
# =============================================================================
#
# ⚠️ 注意: 本项目使用 Conda 环境隔离，每个任务有独立的 environment.yml
# 本文件仅用于文档参考，实际安装请使用:
#
#   bash setup_envs.sh              # 创建所有 10 个环境
#   bash setup_envs.sh --task N     # 创建第 N 个任务的环境
#
# 每个任务的精确依赖定义在:
#   XX_task_name/environment.yml
#
# =============================================================================

# ---------- 以下为全局依赖概览（仅供参考，不要直接 pip install -r） ----------

# ---------- PyTorch 核心（通过 conda 安装，确保 CUDA 版本匹配） ----------
# pytorch          (conda: pytorch channel)
# torchvision      (conda: pytorch channel, 任务 01/02/03/09)
# torchaudio       (conda: pytorch channel, 任务 08)
# pytorch-cuda=12.4 (conda: nvidia channel)

# ---------- 数据科学基础（通过 conda 安装） ----------
# numpy            (conda: defaults)
# pandas           (conda: conda-forge, 任务 10)
# scikit-learn     (conda: conda-forge, 任务 10)
# matplotlib       (conda: conda-forge, 任务 09/10)
# pillow           (conda: conda-forge, 任务 01/02/03/09/10)
# joblib           (conda: conda-forge, 任务 10)

# ---------- HuggingFace 生态（通过 pip 安装，确保最新版本） ----------
# transformers     (pip, 任务 04/05/06/07)
# datasets         (pip, 任务 04/05/06/07)
# accelerate       (pip, 任务 04/05/06/07)
# evaluate         (pip, 任务 04/05/06/07)
# sacremoses       (pip, 任务 05)
# sentencepiece    (pip or conda-forge, required by Marian/opus models, 任务 05)

# ---------- 音频处理（通过 pip 安装） ----------
# librosa          (pip, 任务 08)
# soundfile        (pip, 任务 08)

# ---------- UI 展示（通过 pip 安装） ----------
# gradio           (pip, 所有任务)
